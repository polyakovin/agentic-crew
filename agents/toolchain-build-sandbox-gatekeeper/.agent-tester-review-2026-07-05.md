# Agent Tester Review: toolchain-build-sandbox-gatekeeper

**Date:** 2026-07-05
**Reviewer:** Agent Tester (Hermes Agent)
**Status:** COMPLETE
**Summary:** 13/13 A2A files validated, 3 project-local wrappers checked.
**Verdict:** PASS with minor findings (see below). One actionable issue.

---

## 1. A2A Files (13 of 13) — All Present and Valid

| # | File | Status | Notes |
|---|------|--------|-------|
| 1 | `README.md` | ✅ PASS | Lists all 13 files correctly, documents routes and wrappers |
| 2 | `role.md` | ✅ PASS | 202 lines — detailed mission, scope, non-goals, workflow, report format |
| 3 | `agent-card.json` | ✅ PASS | Valid JSON, 5 skills, all metadata paths resolve from repo root |
| 4 | `harness.yaml` | ✅ PASS | Valid YAML, all paths resolve, boot_probe, quality_gates, release_blockers populated |
| 5 | `entrypoint.md` | ✅ PASS | Comprehensive mission, calibration (overreach/underreach), A2A handoff contract |
| 6 | `workflow.md` | ✅ PASS | 264 lines — full workflow, gate classification, Lua-missing fallback, pdc/SDK diagnostic, githook health, push blocker diagnosis, health snapshot update |
| 7 | `source-map.md` | ✅ PASS | Source hierarchy, toolchain surface map (11 entries), Lua-missing gate diagram, duplicate role check, tainted content boundary |
| 8 | `tool-policy.md` | ✅ PASS | Allowed/forbidden actions, approval gates, file-system policy, network policy, validation policy, git policy, safety rules |
| 9 | `rubric.md` | ✅ PASS | Excellent/Acceptable/Needs Revision/Critical Failure tiers, challenge checklist |
| 10 | `eval-plan.md` | ✅ PASS | 10 seed cases (TB-001 through TB-010), promotion thresholds |
| 11 | `run-record.template.json` | ✅ PASS | Valid JSON, comprehensive schema (44 top-level fields) |
| 12 | `health-snapshot.md` | ✅ PASS | Status: draft, known gaps documented, gate health table (not yet run) |
| 13 | `release-rollback.md` | ✅ PASS | Release blockers list, rollback scope, smoke check commands |

**Path validation:** All 11 harness.yaml paths (`entrypoint`, `role_card`, `agent_card`, etc.) resolve correctly. All 7 agent-card.json metadata paths resolve from repo root. ✓

---

## 2. JSON/YAML Validation

| File | Valid? |
|------|--------|
| `agent-card.json` | ✅ Valid JSON (11 top-level keys, 5 skills) |
| `run-record.template.json` | ✅ Valid JSON (44 top-level keys) |
| `harness.yaml` | ✅ Valid YAML (id=`toolchain-build-sandbox-gatekeeper`, kind=`a2a-specialist-harness`) |
| `manifest.yaml` (Hermes) | ✅ Valid YAML (id=`toolchain-build-sandbox-gatekeeper`, version=`0.1.0`) |
| `playdate-game-crew.yaml` | ✅ Valid YAML (21 agents, 10 routing keys for this agent) |

---

## 3. Non-Goals Consistency Check

Non-goals compared across 5 places where they appear:

| Source | Lines | Key items |
|--------|-------|-----------|
| **role.md** | 14 lines | Full set: no game code mods, no replace spec-contract-guardian/playdate-platform-sdk/qa-reviewer, no new gates, no Makefile changes, no commit/push, no Safe Cracker rename, no interactive simulator, no image analysis, no overwrite dirty |
| **Codex SKILL.md** (`/root/locksmith/.agents/skills/...`) | 13 lines | All core items present. Also adds: `no replace spec-contract-guardian`, `no replace playdate-platform-sdk`, `no replace qa-reviewer` |
| **Hermes skill** (`/root/locksmith/.hermes/skills/...`) | 6 lines | Compact: game code, Makefile/scripts/githooks, commit/push, exit 127, speculate, dirty worktree |
| **Hermes instructions** (`/root/locksmith/.hermes/agents/.../instructions.md`) | 7 lines | Compact: game code, Makefile/scripts/githooks, new gates, commit/push, exit 127, speculate, dirty worktree |
| **manifest.yaml** (`forbidden_actions`) | 9 items | Structured list: game code, Makefile/scripts/githooks, exit 127 misclassify, commit/push, new gates, Safe Cracker rename, image analysis, speculate, overwrite dirty |

**Verdict:** ✅ All consistent. No contradictions. The compact wrappers (Hermes skill, instructions) omit "do not replace other specialists" — acceptable since those are A2A harness concerns.

---

## 4. quality_gates (≥8) in manifest.yaml

**Count:** 12 items.
**Requested minimum:** ≥8. ✅ **PASS**

List:
1. every make target outcome is classified
2. exit 127 is always blocked, never passed or failed
3. make doctor runs first
4. fallback evidence collected when make check blocked by missing Lua
5. pdc/SDK diagnosis references actual env vars and paths
6. githook assessments reference actual hook file content
7. no game code modified during diagnostics
8. simulator quirks (Xvfb on Linux) correctly flagged
9. evidence based on actual command output
10. health-snapshot.md updated after each diagnostic cycle
11. new toolchain failure patterns documented in AGENTS.md
12. no commits or pushes from diagnostic work

---

## 5. forbidden_actions (≥6) in manifest.yaml

**Count:** 9 items.
**Requested minimum:** ≥6. ✅ **PASS**

List:
1. modify game code
2. modify Makefile, scripts, or githooks
3. classify exit 127 as passed or failed
4. commit or push changes
5. create new gates or checks without authorization
6. rename game to Safe Cracker or refer to hero as Safe Cracker
7. analyze images without explicit request
8. speculate about gate outcomes without running actual commands
9. overwrite dirty worktree changes from other agents

---

## 6. Boot Probe — `make doctor` and `make test-all` with `|| true`

Harness.yaml boot_probe commands:

```yaml
boot_probe:
  commands:
    - git status --short
    - cd /root/locksmith && make doctor 2>&1 || true          # ✅ make doctor with ||true
    - cd /root/locksmith && make test-all 2>&1 | tail -10 || true  # ✅ make test-all with ||true
    - cd /root/locksmith && make build 2>&1 | tail -5 || true
```

All 3 make-target commands use `|| true`. ✅ **PASS**

---

## 7. `classify_missing_lua_as` Policy

**Value:** `blocked-gate-not-test-failure` ✅ **PASS**
Present in `harness.yaml` under `boot_probe.classify_missing_lua_as`.

---

## 8. harness.yaml — 12 quality_gates and 14 release_blockers

**Quality gates:** 14 items (requested: ≥12). ✅ **PASS**
**Release blockers:** 14 items (requested: ≥14). ✅ **PASS**

Release blockers (all 14):
1. make_check_blocked_and_no_fallback_evidence
2. lua_missing_classified_as_passed
3. pdc_missing_but_not_diagnosed
4. sdk_dir_misconfigured_and_not_flagged
5. githook_damaged_or_missing_not_reported
6. push_blocker_not_diagnosed
7. health_snapshot_stale_or_not_updated
8. game_code_modified_by_gatekeeper
9. evidence_based_on_speculation_not_command_output
10. commit_or_push_attempted_by_gatekeeper
11. missing_tool_classified_as_test_failure
12. dirty_worktree_overwritten
13. fallback_jq_failed_and_not_reported
14. make_doctor_skipped

---

## 9. Duplicate Role Check — source-map.md

Source-map.md `## Duplicate Role Check` section documents 4 comparisons:

| Agent | Scope | Overlap |
|-------|-------|---------|
| `playdate-platform-sdk` | SDK API correctness, platform fit | None — references "Build Gatekeeper" in its own non-goals, has `build_test_gates` automation pack but focused on SDK calls, not make target execution |
| `spec-contract-guardian` | Spec/code consistency, manifest vs source drift | None — different scope entirely: contracts vs toolchain diagnostics |
| `implementation-engineer` | Scoped code changes | None — modifies code vs runs gates without modifying |
| `qa-reviewer` | Regression, correctness, usability | None — tests game behaviour vs tests toolchain health |

**Verdict:** ✅ **PASS — No actual overlap confirmed.** However, I also checked the **actual playdate-platform-sdk source-map.md** and found:
- Line 30 in `playdate-platform-sdk/role.md`: *"Do not replace the Build Gatekeeper for command execution evidence."* ✓ — references the gatekeeper correctly.
- Line 56 in `playdate-platform-sdk/entrypoint.md`: *"Send generic build infrastructure ownership to a build gatekeeper."* ✓ — proper handoff.

**Minor finding:** The toolchain-build-sandbox-gatekeeper source-map.md references `playdate-platform-sdk` having `automation_packs → build_test_gates`. I verified this is listed in the playdate-platform-sdk source-map's duplicate role check but did not find it in the harness.yaml. This is a stale/misspelled reference — it references the OTHER agent's source-map claim, not a real automation_packs key in playdate-platform-sdk's harness.yaml. This is harmless but the source-map.md should be updated if the claim can't be verified. **(FINDING-001)**

---

## 10. Routing Keys in playdate-game-crew.yaml

**Agent entry:** Present at line 89-91:
```yaml
- id: toolchain-build-sandbox-gatekeeper
  path: ../agents/toolchain-build-sandbox-gatekeeper
  harness: ../agents/toolchain-build-sandbox-gatekeeper/harness.yaml
```

**Routing keys** (10 total):
- `toolchain`, `build`, `sandbox`, `make-gates`, `lua-gate`, `pdc-gate`, `sdk-diagnostic`, `githooks`, `ci-blockers`, `fallback-evidence`

All match the README.md claim of exactly 10 keys. ✅ **PASS**

**Minor finding:** The health-snapshot.md claims "Pack routing for toolchain/build/sandbox keys in playdate-game-crew.yaml" — accurate, there are exactly 3 prefix-level keys (toolchain, build, sandbox) plus 7 more specific ones. ✅

---

## 11. Trailing Whitespace Check

`rg -n "[[:blank:]]$" agents/toolchain-build-sandbox-gatekeeper/ packs/playdate-game-crew.yaml` → exit 1 (no matches). ✅ **All clean.**

---

## 12. Path Verification (Wrappers)

All project-local wrappers exist:

| Wrapper | Path | Status |
|---------|------|--------|
| Codex SKILL | `/root/locksmith/.agents/skills/toolchain-build-sandbox-gatekeeper/SKILL.md` | ✅ |
| Hermes skill | `/root/locksmith/.hermes/skills/toolchain-build-sandbox-gatekeeper.md` | ✅ |
| Hermes package manifests | `/root/locksmith/.hermes/agents/toolchain-build-sandbox-gatekeeper/manifest.yaml` | ✅ |
| Hermes instructions | `/root/locksmith/.hermes/agents/toolchain-build-sandbox-gatekeeper/instructions.md` | ✅ |

---

## Findings Summary

| ID | Severity | Description |
|----|----------|-------------|
| **FINDING-001** | Minor | Source-map.md references `playdate-platform-sdk` having `automation_packs → build_test_gates` — this is a self-referential claim from playdate-platform-sdk's own source-map duplicate role check. It's NOT a real key in playdate-platform-sdk's harness.yaml. The reference is harmless (it's describing what the OTHER agent says about itself) but could be misleading. Consider adding a note that this is a self-reported claim, not independently verified. |
| **FINDING-002** | OK | Hermes skill non-goals (6 items) and instructions non-goals (7 items) are intentionally more compact than role.md (14 items). This is acceptable — the compact wrappers defer to the full A2A harness for details. No contradictions. |

**No critical or major issues found.**

---

## Verdict

| Criterion | Result |
|-----------|--------|
| All 13 A2A files present | ✅ |
| JSON/YAML valid | ✅ |
| Paths resolve | ✅ |
| Non-goals consistent across 5 sources | ✅ |
| quality_gates (≥8): **12** in manifest.yaml | ✅ |
| forbidden_actions (≥6): **9** in manifest.yaml | ✅ |
| `make doctor` with `||true` in boot_probe | ✅ |
| `make test-all` with `||true` in boot_probe | ✅ |
| `classify_missing_lua_as` present | ✅ (`blocked-gate-not-test-failure`) |
| quality_gates (≥12): **14** in harness.yaml | ✅ |
| release_blockers (≥14): **14** in harness.yaml | ✅ |
| Duplicate role check vs playdate-platform-sdk | ✅ (no overlap) |
| Duplicate role check vs spec-contract-guardian | ✅ (no overlap) |
| 10 routing keys in playdate-game-crew.yaml | ✅ |
| Trailing whitespace | ✅ (none) |

**Verdict: PASS.** One minor finding (FINDING-001) — consider clarifying the `automation_packs → build_test_gates` reference in source-map.md if it describes an unverifiable claim. Otherwise the agent is well-structured and ready for pilot.

**Recommendation:** Move from `draft` to `pilot` after addressing FINDING-001 and running at least one real diagnostic cycle to validate the boot_probe and fallback sequences.
