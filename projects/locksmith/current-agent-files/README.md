# Locksmith Agent Files Import

This folder preserves the agent-related files that were previously stored inside the Locksmith repository.

Imported source roots:

- `dot-agents/` mirrors Locksmith `.agents/`.
- `dot-codex/` mirrors Locksmith `.codex/`.
- `dot-hermes/` mirrors Locksmith `.hermes/`.

The portable, project-neutral Playdate specialist lives at `../../agents/playdate-platform-sdk/`.
Use this import as source material when adapting Locksmith-specific rules back into Agentic Crew harnesses.
